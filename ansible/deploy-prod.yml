---
####################################################################################
#  This playbook only builds in dev mode.  
#
####################################################################################


- name: Build Playbook
  hosts: prod
  become: true
  tasks:
                   
  - name: Start running the commands
    import_tasks: discover-tasks.yml


